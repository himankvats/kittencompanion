{
  "name": "@kittencompanion/notification",
  "version": "0.1.0",
  "description": "Notification Lambda — sends reminder and digest emails via SendGrid. See TDD Section 3.1.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "jest --forceExit",
    "test:coverage": "jest --coverage --forceExit",
    "lint": "eslint 'src/**/*.ts'"
  },
  "dependencies": {
    "@aws-sdk/client-sqs": "^3.600.0",
    "@sendgrid/mail": "^8.1.3",
    "aws-lambda": "^1.0.7",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.140",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.14.2",
    "@types/pg": "^8.20.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.5",
    "typescript": "^5.4.5"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testMatch": [
      "**/tests/**/*.test.ts"
    ]
  }
}
